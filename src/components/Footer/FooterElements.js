import styled from '@emotion/styled'

export const FooterP = styled.p`
    text-align: center;
    font-family: 'BenchNine', sans-serif;
    font-size: clamp(.5rem, 1.5vw, .75rem);
`

export const IconDiv = styled.div`
    font-size: clamp(3.5rem, 1.5vw, 3.25rem);
`